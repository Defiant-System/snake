
let Test = {
	init() {
		// setTimeout(() => window.dialog.alert({ message: "test", onOk: () => console.log(123) }), 300);
		// setTimeout(() => snake.dispatch({ type: "window.keystroke", char: "p" }), 500);

		// setTimeout(() => game.setState("new"), 100);
		// setTimeout(() => game.setState("pause"), 200);

		/*
		setTimeout(() => {
			defaultTail = [];
			defaultDir = "s";

			defaultTail = [[20, 15],[20, 14],[20, 13],[20, 12],[20, 11], [20, 10],[20, 9],[20, 8],[20, 7],[20, 6],[20, 5],[20, 4], 
						[20, 3], [19, 3], [18, 3], [17, 3], [16, 3], [15, 3], [14, 3], [13, 3], [12, 3], [11, 3], [11, 4], [11, 5], [11, 6], [11, 7], [11, 8], [11, 9], [11, 10], [11, 11], [11, 12], [11, 13]]
			
			// let ox = 1,
			// 	oy = 1,
			// 	w = 23,
			// 	h = 13;
			// for (let y=0; y<h; y++) {
			// 	for (let x=0; x<w; x++) defaultTail.push([ox+x, oy+y]);
			// 	for (let x=w-1; x>0; x--) defaultTail.push([ox+x, oy+y+1]);
			// }
			// console.log( defaultTail );

			// defaultTail.push([11,6]);
			// defaultTail.push([12,6]);
			// defaultTail.push([13,6]);
			// defaultTail.push([14,6]);
			// defaultTail.push([15,6]);
			// defaultTail.push([15,7]);

			game.setState("new");
		}, 100);
		*/

		// setTimeout(() => game.setState("over"), 3000);
		// setTimeout(() => game.setState("new"), 100);
		// setTimeout(() => game.setState("pause"), 12000);
	}
};

// DEV-ONLY-START
Test.init();
// DEV-ONLY-END
