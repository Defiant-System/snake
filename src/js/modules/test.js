
let Test = {
	init() {
		// setTimeout(() => snake.dispatch({ type: "window.keystroke", char: "p" }), 500);

		// setTimeout(() => game.setState("over"), 3000);
	}
};

// DEV-ONLY-START
Test.init();
// DEV-ONLY-END
